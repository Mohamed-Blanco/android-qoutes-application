import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.Blanco.App',
  appName: 'quotes-app',
  webDir: 'dist/quotes-app/browser/'
};

export default config;
